## area(a) in square.py
Принимает на вход число a - длина стороны квадрата
>def area(a):

Возвращаемое значение - число, площадь квадрата.
Пример вызова:
> def area(3):
>   3 * 3
> area(3) = 9

## perimeter(a) in square.py
Принимает на вход число a - длина стороны квадрата
>def area(a)

Возвращаемое значение - число, периметр квадрата.
Пример вызова:
> def area(3):
>   4 * 3
> area(3) = 12